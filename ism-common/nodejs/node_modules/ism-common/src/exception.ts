export interface Exception<T, S = Error> {
    exception: T,
    source?: S
}

export const createException = <T, S = Error>(exception: T, source?: S): Exception<T, S> => {
    return { exception, source };
};